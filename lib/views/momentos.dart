import 'package:flutter/material.dart';

const Padding contmomentos = Padding(
    padding: EdgeInsets.all(30),
    child: Column(
      children: [
        Text("Momentos Contenedor!"),
      ],
    ));
