import 'package:flutter/material.dart';

const Padding contportada = Padding(
    padding: EdgeInsets.all(30),
    child: Column(
      children: [
        Text("Portada Contenedor!"),
      ],
    ));
